1. 배열의 이름만 전달하는 경우
c
복사
편집
void findMinMax(int a[], int len, int* pmin, int* pmax);
a[]는 배열처럼 보이지만, 사실 함수 인자로 배열 이름을 전달하면 내부적으로 포인터로 변환됩니다. 즉, int a[]는 int* a와 동일한 의미를 가집니다.
즉, 배열 a의 첫 번째 요소의 주소를 전달하는 것이므로, 결국 포인터 int*로 해석됩니다.
